databases:
  - name: note-taking-app-db
    plan: starter

services:
  - type: web
    name: note-taking-app
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: note-taking-app-db
          property: connectionString
